# Optional: keras backend for inference (torch | tensorflow)
KERAS_BACKEND=torch

# Hugging Face model repo (public), e.g. your-username/rice-leaf-disease-model
# Required for Cloud Run when models/ is not bundled in the image.
HF_MODEL_REPO=
